import { Card, Typography } from '@mui/material';
import React from 'react'

const Home = () => {
  return <Card sx={{height: "75vh"}}>
    <h1>Home</h1>
      <Typography>
      フォルダをドロップしてください
        </Typography>
  </Card>
}

export default Home;
